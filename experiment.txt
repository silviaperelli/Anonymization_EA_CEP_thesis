$basePath = ''../{^^.name}/{^^.startTime}/''

ea.experiment(
  runs = [
    ea.run(
      solver = ea.s.nsga2(
        stop = ea.sc.nOfQualityEvaluations(n = 5000);
        representation = ea.r.cfgTree(grammar = ea.grammar.fromFile(path = "src/main/resources/generated-grammar.bnf"));
        mapper = silvia.mapper.treeToQueryMapper()
      );
      problem = silvia.problem.anonymizationProblem(inputCsvPath = "datasets/airQuality.csv");
      randomGenerator = m.defaultRG(seed = 1)
    )
  ];
  listeners = [
    ea.l.console(eFunctions = [
      ea.f.size(of = ea.f.genotype(of = ea.f.best()); format = "%3d")
    ]);
    ea.l.csv(
      path = $basePath + "solutions-percentile.csv";
      eFunctions = [
        f.mapValue(key = privacy; of = ea.f.quality(of = f.percentile(p = 100; of = ea.f.all(); by = f.mapValue(key = privacy; of = ea.f.quality()))));
        f.mapValue(key = "results-similarity"; of = ea.f.quality(of = f.percentile(p = 100; of = ea.f.all(); by = f.mapValue(key = privacy; of = ea.f.quality()))));
        f.mapValue(key = "performance-similarity"; of = ea.f.quality(of = f.percentile(p = 100; of = ea.f.all(); by = f.mapValue(key = privacy; of = ea.f.quality()))));
        ea.f.solution(of = f.percentile(p = 100; of = ea.f.all(); by = f.mapValue(key = privacy; of = ea.f.quality())));

        f.mapValue(key = privacy; of = ea.f.quality(of = f.percentile(p = 100; of = ea.f.all(); by = f.mapValue(key = "results-similarity"; of = ea.f.quality()))));
        f.mapValue(key = "results-similarity"; of = ea.f.quality(of = f.percentile(p = 100; of = ea.f.all(); by = f.mapValue(key = "results-similarity"; of = ea.f.quality()))));
        f.mapValue(key = "performance-similarity"; of = ea.f.quality(of = f.percentile(p = 100; of = ea.f.all(); by = f.mapValue(key = "results-similarity"; of = ea.f.quality()))));
        ea.f.solution(of = f.percentile(p = 100; of = ea.f.all(); by = f.mapValue(key = "results-similarity"; of = ea.f.quality())));

        f.mapValue(key = privacy; of = ea.f.quality(of = f.percentile(p = 100; of = ea.f.all(); by = f.mapValue(key = "performance-similarity"; of = ea.f.quality()))));
        f.mapValue(key = "results-similarity"; of = ea.f.quality(of = f.percentile(p = 100; of = ea.f.all(); by = f.mapValue(key = "performance-similarity"; of = ea.f.quality()))));
        f.mapValue(key = "performance-similarity"; of = ea.f.quality(of = f.percentile(p = 100; of = ea.f.all(); by = f.mapValue(key = "performance-similarity"; of = ea.f.quality()))));
        ea.f.solution(of = f.percentile(p = 100; of = ea.f.all(); by = f.mapValue(key = "performance-similarity"; of = ea.f.quality())));

        f.mapValue(key = privacy; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = privacy; of = ea.f.quality()))));
        f.mapValue(key = "results-similarity"; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = privacy; of = ea.f.quality()))));
        f.mapValue(key = "performance-similarity"; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = privacy; of = ea.f.quality()))));
        ea.f.solution(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = privacy; of = ea.f.quality())));

        f.mapValue(key = privacy; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = "results-similarity"; of = ea.f.quality()))));
        f.mapValue(key = "results-similarity"; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = "results-similarity"; of = ea.f.quality()))));
        f.mapValue(key = "performance-similarity"; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = "results-similarity"; of = ea.f.quality()))));
        ea.f.solution(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = "results-similarity"; of = ea.f.quality())));

        f.mapValue(key = privacy; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = "performance-similarity"; of = ea.f.quality()))));
        f.mapValue(key = "results-similarity"; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = "performance-similarity"; of = ea.f.quality()))));
        f.mapValue(key = "performance-similarity"; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = "performance-similarity"; of = ea.f.quality()))));
        ea.f.solution(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = "performance-similarity"; of = ea.f.quality())))
      ]
    );
    ea.l.savePlotForRun(
      path = $basePath + "scatter-performance-vs-results.svg";
      plot = ea.plot.single.biObjectivePopulation(
        x = f.mapValue(key = "performance-similarity"; of = ea.f.quality());
        y = f.mapValue(key = "results-similarity"; of = ea.f.quality())
      );
      configuration = viz.plot.conf.image(axesShow = all; axesIndependences = [all]);
      secondary = true;
      type = svg
    );
    ea.l.savePlotForRun(
      path = $basePath + "scatter-privacy-vs-results.svg";
      plot = ea.plot.single.biObjectivePopulation(
        x = f.mapValue(key = privacy; of = ea.f.quality());
        y = f.mapValue(key = "results-similarity"; of = ea.f.quality())
      );
      configuration = viz.plot.conf.image(axesShow = all; axesIndependences = [all]);
      secondary = true;
      type = svg
    );
    ea.l.savePlotForRun(
      path = $basePath + "scatter-performance-vs-privacy.svg";
      plot = ea.plot.single.biObjectivePopulation(
        x = f.mapValue(key = "performance-similarity"; of = ea.f.quality());
        y = f.mapValue(key = privacy; of = ea.f.quality())
      );
      configuration = viz.plot.conf.image(axesShow = all; axesIndependences = [all]);
      secondary = true;
      type = svg
    );
    ea.l.savePlotForExp(
      path = $basePath + "p075privacy-results.svg";
      plot = ea.plot.multi.xyExp(
        y = f.mapValue(key = "results-similarity"; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = privacy; of = ea.f.quality()))))
      );
      configuration = viz.plot.conf.image(axesShow = all; axesIndependences = [all]);
      type = svg
    );
    ea.l.savePlotForExp(
      path = $basePath + "p075privacy-performance.svg";
      plot = ea.plot.multi.xyExp(
        y = f.mapValue(key = "performance-similarity"; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = privacy; of = ea.f.quality()))))
      );
      configuration = viz.plot.conf.image(axesShow = all; axesIndependences = [all]);
      type = svg
    );
    ea.l.savePlotForExp(
      path = $basePath + "p075results-privacy.svg";
      plot = ea.plot.multi.xyExp(
        y = f.mapValue(key = "privacy"; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = "results-similarity"; of = ea.f.quality()))))
      );
      configuration = viz.plot.conf.image(axesShow = all; axesIndependences = [all]);
      type = svg
    );
    ea.l.savePlotForExp(
      path = $basePath + "p075results-performance.svg";
      plot = ea.plot.multi.xyExp(
        y = f.mapValue(key = "performance-similarity"; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = "results-similarity"; of = ea.f.quality()))))
      );
      configuration = viz.plot.conf.image(axesShow = all; axesIndependences = [all]);
      type = svg
    );
    ea.l.savePlotForExp(
      path = $basePath + "p075performance-privacy.svg";
      plot = ea.plot.multi.xyExp(
        y = f.mapValue(key = privacy; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = "performance-similarity"; of = ea.f.quality()))))
      );
      configuration = viz.plot.conf.image(axesShow = all; axesIndependences = [all]);
      type = svg
    );
    ea.l.savePlotForExp(
      path = $basePath + "p075performance-results.svg";
      plot = ea.plot.multi.xyExp(
        y = f.mapValue(key = "results-similarity"; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = "performance-similarity"; of = ea.f.quality()))))
      );
      configuration = viz.plot.conf.image(axesShow = all; axesIndependences = [all]);
      type = svg
    )
  ]
)